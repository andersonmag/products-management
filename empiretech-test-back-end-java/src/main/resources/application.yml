spring:
  jackson:
    serialization:
      indent-output: true
  datasource:
    url: jdbc:postgresql://localhost:5432/tenancy1
    username: postgres
    password: admin
  jpa:
    driver-class-name: org.postgresql.Driver
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  cache:
    type: redis

jwt:
  secret-key: 3aa15421-55d3-48f2-8372-ddca66362e98
  token-prefix: Bearer
  token-header: Authorization
  expiration-time: 3600000

server:
  servlet:
    context-path: /api

